package requests

type HTTPClient struct{}

//Get GET请求方法
func (api *HTTPClient) Get(url string, rOpts ...ROpt) {
	//req := NewRequest(rOpts...)

	//response, err := grequests.Get(url, req.toReqOptions())

}
